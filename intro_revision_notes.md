# Introduction 修订说明

## 本次修改

- 修改 `sections/introduction.tex`，形成约 900 词的英文引言；对应中文全文见 `intro_revision_zh.md`。
- 更新 `sections/references.tex`：补充快充综述、2025 年 IEEE TTE 多目标 BO 和 2026 年先验引导充电 BO 文献，补全本轮使用的两个既有条目的 DOI；按正文首次引用排序，移除正文未引用的条目。
- 修改前的引言与参考文献保存在 `tmp/intro_revision/` 中的 `.before` 文件。该备份反映本次操作前的工作区，不以 Git 版本替代用户此前的修改。
- 本次只修订引言和对应引文，中文为审阅材料。方法、实验数据及摘要未作修改。

## 批注来源与回应

Paper 当前 `main.pdf` 没有批注对象；本次依据此前用户提供的 `main(1)(2).pdf`，读取其第 1–2 页批注。批注及对应页面文字保存在 `tmp/intro_revision/intro_annotations.json`。

| 学长提出的问题 | 本次处理 |
|---|---|
| 从快充直接跳到电流，缺少联系 | 先写固定充电量下时间与平均电流的关系，再引出分阶段协议设计。 |
| “SOC 和其他电池状态约束”不清楚 | 明确目标 SOC 是充电终点要求，运行限制具体为电流、电压和温度。 |
| 强化学习与本文无关；MPC 分类不恰当 | 删除 DRL/MPC 旁支综述，集中于昂贵充电评估与 BO。 |
| 没有说清 PDE 仿真和昂贵优化的关系 | 说明仿真减少对实物试验的依赖，但反复求解仍昂贵；BO 减少的是仿真调用次数。 |
| 把少量评估获得好解写成 BO 有效的原因 | 改为概率代理、预测不确定性和采集函数选择的机制解释。 |
| 冷启动篇幅过多，重点应是先验利用 | 两个设计要求分别对应初始协议选择和后续区域偏好的使用。 |
| 高维信赖域文献不足以证明本文过度探索 | 删除 TuRBO、REI 相关段落及未经验证的过度探索论断。 |
| LLM 能力罗列泛泛 | 只讨论任务描述理解和根据优化上下文生成结构化建议；引用 LLAMBO 和电池参数辨识研究。 |
| 框架表述不准确 | 明确 LLM 提议、GP 建模与采集排序、电池仿真评价三个角色。 |

## 文献核验与论断边界

新引言的 11 篇引用已按出版社论文页面、作者机构存档或会议正式论文核对。具体 DOI 及出版信息见中文译文的文献表；本地核验元数据见 `tmp/intro_revision/verified_metadata.json`。

- Attia 的成本节约来自早期预测与 BO 的结合，不能全部归因于 BO。
- Jiang 2022 与 Wang–Jiang 2023 分别支撑受约束快充 BO 和多目标扩展。
- Zhu 2025 考虑膝点处容量及循环寿命，不能改写为温升与充电时间目标。
- Jeong 2026 已研究模型先验引导的充电 BO，因此本文不声称首次在充电 BO 中使用先验。
- LLAMBO 的相关实证任务是超参数优化；Kuai 2025 的任务是电池参数辨识。新引言据此将本文定位到多目标充电协议设计。
- Region-Lift 的贡献表述为机制设计。现有消融为系统层面比较，不声称单独证明后验协方差映射的因果收益。
- Chen2020、Ecker2015 是电池参数化配置；本文结果不被改写成实物电芯寿命或安全保证。

## 原稿中尚需单独核对的事项

1. `sections/abstract.tex` 连续两次提出 LLMBO-MO，并保留 “consistently outperforms conventional BO methods” 的广泛表述。引言已经采用更精确的实验范围；摘要应在单独修订时同步精简。
2. 既有 `refEIMO` 中的 DOI `10.1109/TTE.2026.3610481` 本次未能在线核实：DOI 页面访问失败，Crossref 查询返回 404。这不能证明文章不存在。所提供 EIMO PDF 可用于核对方法内容，但其正式出版元数据仍需作者确认。本次新引言未使用该条目，也未猜测或改动论文其他章节已使用的出版信息。

## 校验

独立审读已确认引言回应上述批注，且贡献表述与现有方法及实验范围一致。正文引用与参考文献按实际 `main.tex` 输入链交叉核对；编译结果和检查记录保存在 `tmp/intro_revision/`。
